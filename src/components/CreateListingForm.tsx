"use client";
import { FormEvent, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
type Territory={id:string;name:string;currency:string}; type Category={id:string;name:string;slug:string;icon?:string|null;parentId?:string|null;parent?:{name:string;slug:string}|null}; type Location={id:string;name:string;territoryId:string};
export default function CreateListingForm({territories,categories,locations}:{territories:Territory[];categories:Category[];locations:Location[]}){
 const router=useRouter(); const [loading,setLoading]=useState(false); const [error,setError]=useState(""); const [territoryId,setTerritoryId]=useState(territories[0]?.id??""); const [categoryId,setCategoryId]=useState(""); const [photoCount,setPhotoCount]=useState(0);
 const currency=useMemo(()=>territories.find(i=>i.id===territoryId)?.currency??"EUR",[territories,territoryId]); const availableLocations=useMemo(()=>locations.filter(i=>i.territoryId===territoryId),[locations,territoryId]); const parents=categories.filter(i=>!i.parentId); const children=categories.filter(i=>i.parentId); const selected=categories.find(i=>i.id===categoryId); const isEvent=selected?.slug.startsWith("evenements-sorties")||selected?.parent?.slug==="evenements-sorties";
 async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setLoading(true);const form=new FormData(event.currentTarget);try{const payload:any={title:form.get("title"),categoryId:form.get("categoryId"),territoryId:form.get("territoryId"),locationId:form.get("locationId")||undefined,price:form.get("price"),description:form.get("description")};if(isEvent){payload.eventStartAt=form.get("eventStartAt");payload.eventEndAt=form.get("eventEndAt");payload.eventVenue=form.get("eventVenue");payload.eventOrganizer=form.get("eventOrganizer");payload.eventUrl=form.get("eventUrl");payload.eventCapacity=form.get("eventCapacity")||undefined;payload.eventIsFree=form.get("eventIsFree")==="on";}const response=await fetch("/api/listings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await response.json();if(!response.ok){setError(data.error??"Impossible de publier l’annonce.");return;}const photos=form.getAll("photos").filter((v):v is File=>v instanceof File&&v.size>0);if(photos.length){const pf=new FormData();photos.forEach(p=>pf.append("photos",p));const pr=await fetch(`/api/listings/${data.listing.id}/images`,{method:"POST",body:pf});if(!pr.ok){const pe=await pr.json();setError(`L’annonce a été créée, mais les photos n’ont pas pu être enregistrées : ${pe.error??"erreur inconnue"}`);return;}}router.push(`/annonces/${data.listing.slug}`);router.refresh();}catch{setError("Impossible de contacter le serveur.");}finally{setLoading(false);}}
 return <form className="listingForm" onSubmit={submit}><div className="listingFormSection"><div className="formSectionTitle"><span>1</span><div><h2>Informations générales</h2><p>Décrivez clairement ce que vous proposez.</p></div></div><label className="fullField"><span>Titre de l’annonce *</span><input name="title" minLength={5} maxLength={120} required /></label><div className="formTwoCols"><label><span>Catégorie *</span><select name="categoryId" required value={categoryId} onChange={e=>setCategoryId(e.target.value)}><option value="" disabled>Sélectionnez une catégorie</option>{parents.map(p=><optgroup label={`${p.icon??""} ${p.name}`} key={p.id}>{children.filter(c=>c.parentId===p.id).map(c=><option value={c.id} key={c.id}>{c.name}</option>)}<option value={p.id}>Autre — {p.name}</option></optgroup>)}</select></label><label><span>Île / territoire *</span><select name="territoryId" required value={territoryId} onChange={e=>setTerritoryId(e.target.value)}>{territories.map(t=><option value={t.id} key={t.id}>{t.name}</option>)}</select></label></div><div className="formTwoCols"><label><span>Ville / commune</span><select name="locationId" defaultValue="" key={territoryId}><option value="">Toute l’île / non précisé</option>{availableLocations.map(l=><option value={l.id} key={l.id}>{l.name}</option>)}</select></label><label className="priceField"><span>Prix *</span><div><input name="price" type="number" min="0" step="0.01" placeholder="0" required/><b>{currency}</b></div></label></div>{isEvent&&<div className="listingFormSection eventFields"><div className="formSectionTitle"><span>📅</span><div><h2>Détails de l’événement</h2><p>Les dates sont enregistrées avec l’événement pour rester synchronisées partout sur Antilles Market.</p></div></div><div className="formTwoCols"><label><span>Date et heure de début *</span><input name="eventStartAt" type="datetime-local" required/></label><label><span>Date et heure de fin *</span><input name="eventEndAt" type="datetime-local" required/></label></div><div className="formTwoCols"><label><span>Lieu / salle</span><input name="eventVenue" maxLength={160}/></label><label><span>Organisateur</span><input name="eventOrganizer" maxLength={120}/></label></div><div className="formTwoCols"><label><span>Lien / billetterie</span><input name="eventUrl" type="url" placeholder="https://..."/></label><label><span>Capacité</span><input name="eventCapacity" type="number" min="1" step="1"/></label></div><label><input name="eventIsFree" type="checkbox"/> Événement gratuit</label></div>}<label className="fullField"><span>Description *</span><textarea name="description" minLength={20} maxLength={5000} rows={9} required/><small>20 caractères minimum. Ne partagez pas d’informations sensibles.</small></label></div><div className="listingFormSection"><div className="formSectionTitle"><span>2</span><div><h2>Photos / affiche</h2><p>Ajoutez jusqu’à 8 images. La première sera l’image principale.</p></div></div><label className="photoDropPlaceholder photoUploadInput"><strong>📷 Ajouter des photos</strong><span>JPG, PNG ou WebP · 10 Mo maximum</span><span className="photoCount">{photoCount?`${photoCount} photo${photoCount>1?"s":""} sélectionnée${photoCount>1?"s":""}`:"Aucune photo sélectionnée"}</span><input name="photos" type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>{const count=e.target.files?.length??0;if(count>8){setError("Vous pouvez sélectionner 8 photos maximum.");e.target.value="";setPhotoCount(0);return;}setError("");setPhotoCount(count);}}/></label></div>{error&&<div className="authError" role="alert">{error}</div>}<div className="listingFormActions"><a href="/" className="secondaryButton">Annuler</a><button className="authSubmit" type="submit" disabled={loading}>{loading?"Publication…":"Publier mon annonce"}</button></div></form>;
}
